<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Welcome to gitPub!</h1>
    <h2>Drink Menu</h2>
    <ul>
        <% for(let i =0; i< allDrinks.length; i++) { 
            %>
            <li>
                <a href="/drinks/<%=i%>"><%=allDrinks[i].name[0].toUpperCase() + allDrinks[i].name.slice(1, allDrinks[i].name.length)%></a>
            </li>
        <% } %>
    </ul>

    <a href="<%=/foods/%>">Food Menu</a>
    <style>
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
</body>
</html>